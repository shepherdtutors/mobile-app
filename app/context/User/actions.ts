// type fcmTokenPayload = {
//   token: string;
// };

export const saveFcmToken = async (): // payload?: fcmTokenPayload,
Promise<void> => {
  // const user_id = await savedUser();
  // if (isEmpty(user_id) || isNil(user_id)) {
  //   return;
  // }
  // if (isEmpty(user_id) || isNil(user_id) || isNull(user_id)) {
  //   return Promise.reject('missing user id');
  // }
  // await API.post(`user/${user_id}/pn-token`, payload);
};
